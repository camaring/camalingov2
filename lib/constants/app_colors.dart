import 'package:flutter/material.dart';

/// Centralized color palette for the Camalingo app to maintain
/// consistent theming across all UI components.
class AppColors {
  /// Pure white color, typically used for backgrounds and surfaces.
  static const Color white = Color(0xFFFFFFFF);
  /// Pure black color, used for primary text and icons.
  static const Color black = Color(0xFF000000);
  /// Primary brand color (green), used for buttons, highlights, and accents.
  static const Color primaryGreen = Color(0xFF4CAF50);
  /// Light grey background color for cards and secondary surfaces.
  static const Color backgroundGrey = Color(0xFFF5F5F5);
}
